<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MedCheck" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style
        name="Theme.Google.SignIn" parent="Theme.AppCompat.DayNight.NoActionBar">
    </style>

    <style name="Theme.MedCheck" parent="Base.Theme.MedCheck" />

    <style name="nightModeSwitch" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@color/night_mode</item>
        <item name="colorSwitchThumbNormal">#FFFFFF</item>
        <item name="android:colorForeground">#BFBABA</item>
    </style>

    <style name="textView_style">
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- For bottom nav -->
    <style name="ActiveTabTextStyle" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textColor">@color/textBlue</item>
    </style>

    <style name="InactiveTabTextStyle" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textColor">@color/preferences_text</item>
    </style>


</resources>